/**
 * npm이란?
 * 
 * Node Package Manager
 * 다른 사람들이 만든 소스 코드들을 모아둔 저장소
 */
/**
 * package.json
 * 현재 프로젝트에 대한 정보와 사용 중인 패키지에 대한 정보를 담은 파일
 * $ npm init
 * $ npm i (설치할 패키지)
 * $ npm i -D (개발할 때만 쓸 패키지 설치)
 * $ npm i -g (전역설치 -> 디팬던시에 기록이 안됨)
 * $ npm run (스크립트)
 * 
 * npx rimraf
 * 
 * 글로벌 설치는 최대한 기피하는 게 좋음
 */

/**
 * SemVer 버저닝
 * - 노드 패키지의 버전은 SemVer(유의적 버저닝) 방식을 따름
 * - 메이저(하위 호환이 되지 않는 변경 사항)
 * - 마이너
 * - 패치
 */
/**
 * npm search
 * npm info
 * npm whoami
 * npm ls
 * npm publish
 * npm unpublish --force
 */